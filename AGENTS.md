# OVERTIME: AI Agent Working Agreement

このプロジェクトでは、依頼を受けたエージェントはまず `tasks/` に作業を記録し、
独立して実装できるものは Git worktree で並列化します。タスクの状態と完了条件が、
人間とエージェント間の唯一の進捗記録です。

## 依頼を受けたときの手順

ユーザーが「これをやって」と依頼したら、次の順で進めます。

1. `scripts/task.sh create` でタスクを作る（既存タスクなら再利用する）。
2. `scripts/task.sh next` とタスク本文を確認し、変更範囲を決める。
3. 他のタスクとファイル競合しない作業だけを、各エージェント専用 worktree で実装する。
4. タスクに書かれた検証を実行し、結果をタスク本文の「実装メモ」に残す。
5. レビュー可能な変更だけを main に統合し、`done` にする。未検証なら `review` にする。

ユーザーがタスク化を明示しなくても、複数ステップ以上の実装・調査はタスク化する。
短い質問だけはタスク化しない。

## 並列作業の境界

- `main.gd` は当面、**一度に一つの実装タスクだけ**が変更してよいホットスポット。
- 新機能は可能な限り `scripts/`、`docs/`、または新しい `gameplay/` 配下の専用ファイルへ分離する。
- アセット追加は `assets/` 内で専用サブディレクトリを使い、既存アセットを上書きしない。
- `project.godot`、`main.tscn`、`main.gd` の変更が必要なタスクは、タスクの「変更予定ファイル」に必ず明記する。
- `.godot/` と `.agent-worktrees/` は生成物であり、編集・コミットしない。

## 完了の基準

完了前に、少なくとも以下を記録する。

- 変更したファイル
- 実行した検証コマンドと結果
- 手動確認が必要な項目
- 他タスクへ引き継ぐ注意点

Godot CLI が利用可能なら、基本の構文・ロード検証は
`godot --headless --path . --editor --quit` を用いる。利用できない環境では、その旨を記録して
Godot Editor での確認手順を書く。
