interface:
  display_name: "OVERTIME Task Orchestration"
  short_description: "OVERTIME専用の実行しないタスク作成ワークフロー"
  default_prompt: "$overtime-task-orchestration を使って、この依頼をタスク化してください。"

policy:
  allow_implicit_invocation: true
